using System;
using System.Collections.Generic;
using UnityEngine;

// An unit action represents something an unit can do. For each action, there is a corresponding command that can be issued to instruct the unit to
// perform the action.

/*
    Example of actions :
        - Move to a certain position
        - Move in a certain direction
        - Rotate to a certain direction
        - Use a certain ability
        - Interact with something
        - Pick-up an object
        - Perform something in the inventory (drop sopething, move something in the inventory, stack objects of the inventory, etc)
        - Use an item (consumable from the inventory)
*/
public class Unit_Action { }